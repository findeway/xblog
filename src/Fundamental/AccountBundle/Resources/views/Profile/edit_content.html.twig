<div class="row">
	<div class="col-md-offset-3 col-md-6">
		<form action="{{ path('fos_user_profile_edit') }}"
			 {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit">
			{{ form_widget(form) }}
			<div>
				<input type="submit" class="btn btn-warning form=control"
					value="{{ 'save'|trans({}, 'FOSUserBundle') }}" />
			    <a class="pull-right" href="{{ path('fos_user_change_password') }}" ><strong class="btn btn-warning">change password</strong></a>
			</div>
		</form>
	</div>
</div>
